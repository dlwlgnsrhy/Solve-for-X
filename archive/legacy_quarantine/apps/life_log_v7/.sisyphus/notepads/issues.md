# Issues & Gotchas

- Existing widget_test.dart has default counter test — must be replaced in T12
- Existing main.dart has default Flutter demo — must be replaced in T12
- lib/data/ already has `datasources/` subdirectory from previous setup
- Plan says "TDD test-first" but TDD tests are written BEFORE feature; we write failing tests first
